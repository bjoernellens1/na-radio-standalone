# Encoder settings for docker-compose
# Uncomment or edit values as needed.

# Force encoder to use CUDA even on legacy GPUs (e.g., Tesla P100).
ENCODER_DEVICE=cuda
FORCE_GPU=1
MIN_CC=6.0

# Base image for Docker builds; override to match your GPU (P100 uses CUDA 11.6).
PYTORCH_BASE_IMAGE=pytorch/pytorch:1.13.1-cuda11.6-cudnn8-runtime
