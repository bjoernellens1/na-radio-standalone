# Minimal Python requirements for the na-radio webcam demo
# Minimal Python requirements for the na-radio webcam demo
# torch and torchvision are installed by the setup scripts/Dockerfile to match the selected runtime (CPU vs CUDA)
# They are deliberately removed to avoid accidental installation of mismatched GPU wheels
numpy<2; python_version < "3.13"
numpy>=2.0; python_version >= "3.13"
opencv-python-headless
matplotlib
timm
typing-extensions
einops
open_clip_torch==2.24.0
imageio
flask
gunicorn
werkzeug
transformers
ultralytics
segment-anything
git+https://github.com/ultralytics/CLIP.git
pyyaml

# Intel Optimizations
# Note: Specific versions might be needed depending on hardware/OS, 
# but we'll start with standard pip packages.
# For XPU support, one might need --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
# But for now we assume CPU or standard IPEX.
intel-extension-for-pytorch
openvino>=2024.0.0



